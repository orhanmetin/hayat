namespace Hayat.Infrastructure.Data
{
    /// <summary>Logged at startup so production can verify the running binary.</summary>
    public static class BootstrapVersion
    {
        public const string Id = "v5-pusula-day-review-snapshot-20260801";
    }
}
